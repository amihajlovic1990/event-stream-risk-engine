[project]
name = "event-stream-risk-engine"
version = "0.1.0"
description = "Event-time risk scoring reference implementation"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.5"]

[tool.pytest.ini_options]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
